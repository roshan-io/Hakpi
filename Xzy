<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Expanding Menu</title>
    <style>
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
        }
        .top-bar {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            background-color: black;
            padding: 15px 10px; /* Adjustable height */
            display: flex;
            justify-content: flex-end;
            align-items: center;
            z-index: 50;
            box-sizing: border-box;
        }
        .menu-icon {
            color: white;
            font-size: 24px;
            cursor: pointer;
            border: none;
            background: none;
            padding: 10px;
        }
        .menu-panel {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            background-color: black;
            color: white;
            overflow: hidden;
            max-height: 0; /* Initial state */
            transition: max-height 0.4s ease-in-out;
            box-sizing: border-box;
            z-index: 100;
        }
        .menu-panel ul {
            list-style: none;
            padding: 10px;
            margin: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        .menu-panel li {
            padding: 10px;
            cursor: pointer;
            width: 100%;
            text-align: center;
        }
        .menu-panel li:hover {
            color: #bbb;
        }
        .overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100vh;
            background: rgba(0, 0, 0, 0.5);
            backdrop-filter: blur(5px);
            z-index: 99;
            display: none;
        }
        .content {
            padding: 100px 20px;
            text-align: center;
            transition: filter 0.3s ease-in-out;
        }
        .blurred {
            filter: blur(5px);
        }
    </style>
    <script>
        function toggleMenu(show) {
            const menu = document.getElementById("menuPanel");
            const menuIcon = document.getElementById("menuIcon");
            const overlay = document.getElementById("overlay");
            const content = document.getElementById("content");
            const topBar = document.querySelector(".top-bar");

            let topBarHeight = topBar.offsetHeight; // Get dynamic height

            if (show) {
                menu.style.maxHeight = "300px"; // Expand smoothly
                menuIcon.style.display = "none";
                overlay.style.display = "block";
                content.classList.add("blurred");
            } else {
                menu.style.maxHeight = "0";
                menuIcon.style.display = "block";
                overlay.style.display = "none";
                content.classList.remove("blurred");
            }
        }
    </script>
</head>
<body>
    <div class="top-bar">
        <button id="menuIcon" class="menu-icon" onclick="toggleMenu(true)">☰</button>
    </div>
    <div id="overlay" class="overlay" onclick="toggleMenu(false)"></div>
    <div id="menuPanel" class="menu-panel">
        <ul>
            <li onclick="toggleMenu(false)">Home</li>
            <li onclick="toggleMenu(false)">About</li>
            <li onclick="toggleMenu(false)">Services</li>
            <li onclick="toggleMenu(false)">Contact</li>
        </ul>
    </div>
    <div id="content" class="content">
        <h1>Welcome to the Website</h1>
        <p>This is some sample content to demonstrate the blur effect when the menu is opened.</p>
    </div>
</body>
</html><!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Expanding Menu</title>
    <style>
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
        }
        .top-bar {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            background-color: black;
            padding: 15px 10px; /* Adjustable height */
            display: flex;
            justify-content: flex-end;
            align-items: center;
            z-index: 50;
            box-sizing: border-box;
        }
        .menu-icon {
            color: white;
            font-size: 24px;
            cursor: pointer;
            border: none;
            background: none;
            padding: 10px;
        }
        .menu-panel {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            background-color: black;
            color: white;
            overflow: hidden;
            max-height: 0; /* Initial state */
            transition: max-height 0.4s ease-in-out;
            box-sizing: border-box;
            z-index: 100;
        }
        .menu-panel ul {
            list-style: none;
            padding: 10px;
            margin: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        .menu-panel li {
            padding: 10px;
            cursor: pointer;
            width: 100%;
            text-align: center;
        }
        .menu-panel li:hover {
            color: #bbb;
        }
        .overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100vh;
            background: rgba(0, 0, 0, 0.5);
            backdrop-filter: blur(5px);
            z-index: 99;
            display: none;
        }
        .content {
            padding: 100px 20px;
            text-align: center;
            transition: filter 0.3s ease-in-out;
        }
        .blurred {
            filter: blur(5px);
        }
    </style>
    <script>
        function toggleMenu(show) {
            const menu = document.getElementById("menuPanel");
            const menuIcon = document.getElementById("menuIcon");
            const overlay = document.getElementById("overlay");
            const content = document.getElementById("content");
            const topBar = document.querySelector(".top-bar");

            let topBarHeight = topBar.offsetHeight; // Get dynamic height

            if (show) {
                menu.style.maxHeight = "300px"; // Expand smoothly
                menuIcon.style.display = "none";
                overlay.style.display = "block";
                content.classList.add("blurred");
            } else {
                menu.style.maxHeight = "0";
                menuIcon.style.display = "block";
                overlay.style.display = "none";
                content.classList.remove("blurred");
            }
        }
    </script>
</head>
<body>
    <div class="top-bar">
        <button id="menuIcon" class="menu-icon" onclick="toggleMenu(true)">☰</button>
    </div>
    <div id="overlay" class="overlay" onclick="toggleMenu(false)"></div>
    <div id="menuPanel" class="menu-panel">
        <ul>
            <li onclick="toggleMenu(false)">Home</li>
            <li onclick="toggleMenu(false)">About</li>
            <li onclick="toggleMenu(false)">Services</li>
            <li onclick="toggleMenu(false)">Contact</li>
        </ul>
    </div>
    <div id="content" class="content">
        <h1>Welcome to the Website</h1>
        <p>This is some sample content to demonstrate the blur effect when the menu is opened.</p>
    </div>
</body>
</html>
